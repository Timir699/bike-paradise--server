const User = require('./user');
const BusinessPackages = require('./businessPackages')
const Order = require('./order')
const Review = require('./review')

const models = {
    User,
    BusinessPackages,
    Order,
    Review
}

module.exports = models;